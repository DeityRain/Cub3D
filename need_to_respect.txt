I dont have the right too use mlx_pixel_put (like you did on FDF) because its way too slow for that project.

You gonna have too use an Image Buffer.

instead of Drawing pixels one by one on the Screen you Will:

- Create a Virtual Image in memory
- write all your pixel colors into that memory Buffer.
- push the entier image to the window in one go


mini goal :

Best next milestone

1 - Create an image buffer module

init MLX image
get pixel data pointer
write pixels in memory
render the whole image to the window
destroy the image cleanly on exit

2 - Add a basic render loop

clear screen
draw ceiling/floor
display buffer

3 - Then implement raycasting

cast one ray per screen column
find wall hit
draw wall slice

4 - After that

player movement
collision detection
textures
minimap if needed