I dont have the right too use mlx_pixel_put (like you did on FDF) because its way too slow for that project.

You gonna have too use an Image Buffer.

instead of Drawing pixels one by one on the Screen you Will:

- Create a Virtual Image in memory
- write all your pixel colors into that memory Buffer.
- push the entier image to the window in one go


Where I am Now : 


mini goal :

1- Test running ./cub3D maps/miniMap.cub to confirm the full pipeline works end-to-end.

2 - Implement map enclosure check (flood-fill validation that walls are closed).

3 - Start rendering (initialize raycasting, display window content).



====================================================================================

1 - Finish map parsing

    validate the .cub file structure
    extract textures, colors, and the map grid

2 - Validate the map

    exactly one player start
    only valid characters
    map closed by walls
    no empty space leaks

3 - Store the game state cleanly

    create a main structure for textures, map, player, and MLX data
    load all settings from the file into it

4 - Set up rendering and player movement

    initialize the window
    draw the initial view
    handle key input for movement and rotation

5 - Implement raycasting and display

    cast rays for each screen column
    compute wall distances
    draw vertical wall slices and update the frame


====================================================================================

im afraid of all the map_extraction file, i need too study